{
  "name": "sensei",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.0",
  "description": "Local-first mentoring and workflow analysis over normalized Claude and Codex history.",
  "module": "./src/index.ts",
  "scripts": {
    "lint": "biome lint .",
    "sensei": "bun ./src/cli/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run lint && bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.5",
    "@types/bun": "latest",
    "typescript": "^5.8.2"
  }
}
